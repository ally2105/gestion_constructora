@model IEnumerable<Firmeza.Core.Models.Cliente>

@{
    ViewData["Title"] = "Gestión de Clientes";
}

<div class="card-admin card-admin-enhanced">
    <div class="card-header card-header-enhanced">
        <div class="header-left">
            <i class="fas fa-users icon-header"></i>
            <div>
                <h2 class="title">Gestión de Clientes</h2>
                <p class="subtitle">Administre la información de todos los clientes registrados.</p>
            </div>
        </div>
        <div class="header-right">
            <a asp-action="Create" class="btn-admin btn-save">
                <i class="fas fa-user-plus"></i> Crear Nuevo Cliente
            </a>
        </div>
    </div>

    <div class="form-content">
        <div class="d-flex justify-content-end mb-3">
            <form asp-action="Index" method="get" class="d-flex">
                <input type="text" name="searchTerm" class="form-control-admin" placeholder="Buscar por nombre o documento..." style="margin-right: 10px;"/>
                <button type="submit" class="btn-admin-secondary">
                    <i class="fas fa-search"></i> Buscar
                </button>
            </form>
        </div>

        @if (!Model.Any())
        {
            <p style="color: var(--color-text-secondary); text-align: center; margin-top: 20px;">No hay clientes registrados.</p>
        }
        else
        {
            <table class="table-admin">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Email</th>
                        <th>Dirección</th>
                        <th>Fecha de Registro</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.Usuario.Nombre)</td>
                            <td>@Html.DisplayFor(modelItem => item.Usuario.Email)</td>
                            <td>@Html.DisplayFor(modelItem => item.Direccion)</td>
                            <td>@Html.DisplayFor(modelItem => item.FechaRegistro)</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn-admin-secondary btn-sm" title="Editar">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn-admin-secondary btn-sm" style="margin-left: 5px;" title="Detalles">
                                    <i class="fas fa-info-circle"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn-admin-danger btn-sm" style="margin-left: 5px;" title="Eliminar">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</div>
