@model gestion_construccion.web.Models.ViewModels.VentaViewModel

@{
    ViewData["Title"] = "Crear Nueva Venta";
}

<div class="card-admin card-admin-enhanced">
    <div class="card-header card-header-enhanced">
        <div class="header-left">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon-header">
                <circle cx="9" cy="21" r="1"></circle>
                <circle cx="20" cy="21" r="1"></circle>
                <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
            </svg>
            <div>
                <h2 class="title">Crear Nueva Venta</h2>
                <p class="subtitle">Registre una nueva transacción de venta en el sistema.</p>
            </div>
        </div>
    </div>

    <form asp-action="Create">
        <div class="form-content">
            <div asp-validation-summary="All" class="validation-summary-errors"></div>

            <h4 style="color: var(--color-primary); margin-top: 0; margin-bottom: 20px;">Detalles de la Venta</h4>
            
            <div class="form-group-admin">
                <label asp-for="ClienteId"></label>
                <select asp-for="ClienteId" class="form-control-admin" asp-items="ViewBag.ClienteId">
                    <option value="">-- Seleccione un Cliente --</option>
                </select>
                <span asp-validation-for="ClienteId" class="text-danger"></span>
            </div>

            <div class="form-group-admin">
                <label asp-for="FechaVenta"></label>
                <input asp-for="FechaVenta" class="form-control-admin" type="datetime-local" />
                <span asp-validation-for="FechaVenta" class="text-danger"></span>
            </div>

            <h4 style="color: var(--color-primary); margin-top: 30px; margin-bottom: 20px;">Productos de la Venta</h4>
            
            <div class="form-group-admin">
                <label asp-for="ProductoId"></label>
                <select asp-for="ProductoId" class="form-control-admin" asp-items="ViewBag.ProductoId">
                    <option value="">-- Seleccione un Producto --</option>
                </select>
                <span asp-validation-for="ProductoId" class="text-danger"></span>
            </div>

            <div class="form-group-admin">
                <label asp-for="Cantidad"></label>
                <input asp-for="Cantidad" class="form-control-admin" type="number" min="1" />
                <span asp-validation-for="Cantidad" class="text-danger"></span>
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-admin btn-save">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
                Crear Venta
            </button>
            <a asp-action="Index" class="btn-admin-secondary btn-cancel">
                <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <line x1="18" y1="6" x2="6" y2="18"></line>
                    <line x1="6" y1="6" x2="18" y2="18"></line>
                </svg>
                Cancelar
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
